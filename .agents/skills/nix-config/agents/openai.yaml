interface:
  display_name: "Nix Config"
  short_description: "Repo-specific patterns and workflows for nix-config"
  default_prompt: "Use the nix-config repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true